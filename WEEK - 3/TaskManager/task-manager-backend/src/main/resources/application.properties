# ============================================================
# Task Manager — Application Properties
# ============================================================

# Server port (default is 8080)
server.port=8080

# Application name (appears in logs and actuator info)
spring.application.name=task-manager
