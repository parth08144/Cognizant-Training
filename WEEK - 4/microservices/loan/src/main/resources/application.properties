# Hands-on note: launching loan with the default port 8080 while account is
# already running fails with "port already in use" - hence port 8081 here.
server.port=8081

# Additional (Eureka registration): name shown in the discovery registry
spring.application.name=loan-service
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
